# Base URL of the Spring Boot backend API. Leave empty ("") to use relative paths
# (recommended when the frontend is served by the same origin/proxy as the backend,
# or when using the Vite dev-server proxy configured in vite.config.js).
VITE_API_BASE=
